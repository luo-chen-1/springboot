spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot_data?serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: wjjwjjwjj
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis:
  configuration:
    map-underscore-to-camel-case: true #配置驼峰映射
  mapper-locations: classpath:mapper/*.xml #配置
  type-aliases-package: com.wjj.domain
